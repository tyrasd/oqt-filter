{
  "name": "oqt-filter",
  "version": "0.0.0",
  "description": "tile-reduce job that filters osmqatiles and enhances it with external data",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "tile-reduce": "^3.1.1"
  }
}
